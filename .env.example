# .env 파일은 Git에 올리면 안 됨!
# .env.example 파일을 만들어서 형식만 공유

# .env.example 생성
cat > .env.example << EOF
# API 서버 주소
VITE_API_URL=http://localhost:8080

# 기타 환경변수 (값은 비워둠)
# VITE_GOOGLE_API_KEY=
# VITE_KAKAO_API_KEY=
EOF

# .env.example은 Git에 추가
git add .env.example
git commit -m "docs: 환경변수 예시 파일 추가"